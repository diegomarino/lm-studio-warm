# opencode-lmstudio-warm

This package has been renamed to [`opencode-lm-studio-warm`](https://www.npmjs.com/package/opencode-lm-studio-warm) — install that instead. The old name keeps working via this re-export shell.
