import type { ExtensionAPI } from "@oh-my-pi/pi-coding-agent"

/** omp extension entry. Full wiring lands in a later task. */
export default function lmStudioWarm(_pi: ExtensionAPI): void {
  // inactive stub until config + gate land
}
