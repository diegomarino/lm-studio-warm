node_modules/
dist/
*.log
.DS_Store
.cache/
coverage/
*.tsbuildinfo
# scratch recordings (the shipped demo cast docs/assets/quickstart-omp.cast IS tracked)
demo-test.cast

# opencode runtime state the live e2e can leave behind (change audit X4)
.auto-update-history.json

# local maintainer workspaces — audits, superpowers specs/plans/launchers
docs/audits/
docs/superpowers/

# temporal maintainer docs for the initial publish — stay local
/RELEASING.md
/NEXT-STEPS.md

# repo-level state (main): linked worktrees and agent session dirs
.worktrees/
.superpowers/
.remember/
